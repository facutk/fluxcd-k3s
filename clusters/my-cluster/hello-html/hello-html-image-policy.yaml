apiVersion: image.toolkit.fluxcd.io/v1beta1
kind: ImagePolicy
metadata:
  name: hello-html
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: hello-html
  policy:
    semver:
      range: ^1.x-0